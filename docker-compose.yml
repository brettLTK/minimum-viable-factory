services:
  factory:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./memory:/app/memory
      - ./.claude:/app/.claude
      - ./audit:/app/audit
      - ./workspace:/app/workspace
    env_file:
      - .env
